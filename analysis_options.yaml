include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Generated files
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.gr.dart"
    - "**/*.config.dart"
    - "**/*.gen.dart"
    - "lib/gen/**"
    - "lib/generated/**"
    - "lib/localization/generated/**"

  errors:
    invalid_annotation_target: ignore   # freezed needs this
    missing_required_param: error       # ← was 'true', must be error/warning/info
    invalid_assignment: error           # ← was 'true', must be error/warning/info
    unused_import: warning              # ← was 'ignore', better as warning
    unused_local_variable: warning

  language:
    strict-casts: true        # no implicit casts
    strict-inference: true    # no untyped closures/variables
    strict-raw-types: true    # no raw List, Map, Box etc.

linter:
  rules:
    # —— Safety ——————————————————————————————————
    avoid_print: true
    avoid_returning_null_for_void: true
    avoid_dynamic_calls: true
    unawaited_futures: true
    unrelated_type_equality_checks: true
    null_check_on_nullable_type_parameter: true
    unnecessary_await_in_return: true
    unnecessary_null_checks: true
    unnecessary_nullable_for_final_variable_declarations: true
    invalid_case_patterns: true
    cancel_subscriptions: true
    close_sinks: true

    # —— Style ————————————————————————————————————
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    always_declare_return_types: true
    always_put_required_named_parameters_first: true
    always_put_control_body_on_new_line: true
    sort_constructors_first: true
    use_super_parameters: true
    use_key_in_widget_constructors: true
    use_enums: true
    use_decorated_box: true
    sized_box_for_whitespace: true
    cascade_invocations: true
    require_trailing_commas: true
    unnecessary_this: true
    directives_ordering: true

    # —— Types ————————————————————————————————————
    always_specify_types: true

    # —— Docs —————————————————————————————————————
    public_member_api_docs: false       # too noisy for app code

    # —— Disabled —————————————————————————————————
    lines_longer_than_80_chars: false   # handled by formatter